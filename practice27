//Метод printVars() является статическим, поэтому он может вызываться через:

//Имя класса:

A.printVars();

//Экземпляр класса (не рекомендуется, но возможно):

A instance = new A();
instance.printVars();
